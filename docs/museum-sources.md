# 馆藏素材源

2.1.0 新增 V&A、芝加哥艺术博物馆、克利夫兰艺术博物馆。三者通过各自官方公开接口检索，无需账号、API 密钥、OpenCLI 或浏览器登录。沿用现有 `TY Image Spider 2.0` 工作流即可。

## 浏览方式

| 来源 | 推荐用途 | 默认分类 | 检索示例 |
| --- | --- | --- | --- |
| V&A | 摄影、时装、海报、装饰艺术、纹样 | 摄影 | 摄影分类下输入 `landscape` |
| 芝加哥艺术 | 绘画、平面设计、建筑图纸、雕塑 | 绘画 | 绘画分类下输入 `Monet` |
| 克利夫兰 | 摄影、绘画、素描、版画、纺织 | 摄影 | 摄影分类下输入 `portrait` |

分类选项使用中文，映射到来源的真实分类 ID 或类型名称。关键词留空即可浏览；作者、作品名及说明来自原馆藏记录，主要为英文，当前不做自动翻译。

每页请求24件作品；来源记录缺少可用图片时跳过该记录，仍按真实分页信息翻页，所以个别页面可能不足24件。切换来源保留本会话中的已加载结果与上下页状态。

详情中展示作品作者、年代、媒介、尺寸、说明和使用条件等来源实际提供的字段。馆藏作品不会显示 AI 提示词徽标。点击详情图片可进入全屏，初始展示全图，滚轮缩放、拖拽平移、双击复位。

## 图片与缓存

- V&A 当前使用作品主图，尚未导入单件作品的所有附图。高清标记的作品预览最长边800像素、下载最长边1680像素；低分辨率作品使用来源提供的原始尺寸。
- 芝加哥艺术使用 IIIF 图片，预览宽843像素；公共领域作品下载宽1686像素，其他作品宽843像素。详情展示公共领域或来源版权说明。
- 克利夫兰预览使用 `web` 图，下载优先使用 `print` JPG；不默认下载体积较大的完整 TIFF。
- “新增缓存100张”只计算新增成功的作品。每件存一张预览与完整详情；已有缓存跳过，当前条件保存断点，下一次点击继续。缓存任务在后端异步执行，切换来源不影响任务。
- 预览缓存按来源与作品 ID 索引。再次检索命中后使用本地 `/view` 地址，详情也复用本地记录；高清大图仍按需联网获取。搜索结果列表本身仍需来源接口，不等同于完整离线馆藏。
- 缓存放在 `output/ty-node/ty-image-spider/cache/`，索引为 `index.sqlite3`。手动下载按来源分别存入 `vam/`、`artic/`、`cleveland/`，下载成功后节点会显示完整绝对路径。

## 模块职责

- `MuseumClient`：固定官方域名的 JSON 请求、响应限制、错误转换与五分钟元数据缓存。
- `VamProvider`、`ArticProvider`、`ClevelandProvider`：各自分类映射、分页规则、字段转换与详情策略。
- `museum_assets`：小型纯函数，负责页码、分类、纯文本与馆藏详情校验。
- `CuratedDownloader`：可信图片域名、图片格式和保存路径校验；保留 IIIF 尺寸路径的逗号与感叹号。
- 现有 `AssetIndex`、`CacheJobService`：持久索引与后台缓存，不引入博物馆专属任务分支。
- `collection_detail.js`：独立渲染馆藏资料，异步更新文字时保留当前图片查看器。

## 官方接口依据

- [V&A API v2](https://developers.vam.ac.uk/guide/v2/welcome.html)，[IIIF 图片](https://developers.vam.ac.uk/guide/v2/images/iiif.html)
- [芝加哥艺术博物馆 API](https://api.artic.edu/docs/)
- [克利夫兰艺术博物馆 Open Access API](https://openaccess-api.clevelandart.org/)

## 2026-09-20 实测

三家首屏和第二页各24项，两页之间无重复；切换摄影、时装、绘画或雕塑分类后结果发生对应变化。真实关键词 `landscape`、`Monet`、`portrait` 均返回24项，样图下载和详情成功。

样图包含 V&A `O499248`（1680×1260 JPEG）、芝加哥 `286372`（843×588 JPEG）与克利夫兰 `160087`（2589×3400 JPEG）。本机保存目录为 `C:/path/to/ComfyUI/output/ty-node/ty-image-spider/` 下各来源子目录。

三家均实测完整新增缓存100张：V&A 失败1项、芝加哥艺术失败6项后继续补足；克利夫兰无失败。V&A 重新检索首屏24件中23件复用本地 `/view` 预览，已验证本地图片返回200。

V&A 第二次点击从断点续存，新增8件、跳过已有5件后取消；索引从100件增长到108件，取消状态已确认，已缓存内容保留。

部分芝加哥图片传输存在截断现象，例如实测中《睡莲》的843像素预览读取不完整，而1686像素下载成功。外部图片或详情可能失败；批量缓存记录失败数并继续处理其他作品，不会把失败项计入新增成功数量。
